package cl.uchile.dcc.citric
package model.norm

import model.units.PlayerCharacter

class Norma5Test extends munit.FunSuite {/*
  private var norma: Norma5 = _
  private var player: PlayerCharacter = _

  override def beforeEach(context: BeforeEach): Unit = {
    norma = new Norma5
    player = new PlayerCharacter("Molly", 5, 1, 1, 0)
  }

  test("Norma5 boost to Norma6 by stars objective") {
    player.increaseStars(200)
    player.changeRoad(1)
    assert(norma.checkBoost(player).isInstanceOf[Norma6])
  }

  test("Norma5 boost to Norma6 by victories objective") {
    player.increaseVictories(14)
    player.changeRoad(2)
    assert(norma.checkBoost(player).isInstanceOf[Norma6])
  }

  test("Norma5 don't boost to Norma6 by stars objective") {
    player.changeRoad(1)
    assert(norma.checkBoost(player).isInstanceOf[Norma5])
  }

  test("Norma5 don't boost to Norma6 by victories objective") {
    player.changeRoad(2)
    assert(norma.checkBoost(player).isInstanceOf[Norma5])
  }

  test("Norma5 give his value as an int") {
    val value: Int = norma.toNumber()
    assertEquals(value, 5)
  }*/
}
